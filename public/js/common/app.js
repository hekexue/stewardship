/*
负责初始化路由配置，模块激活、禁用机制，同时负责大型单页面模块延迟加载
 */
define(["./ClassBase"], function(Class) {

	var App = Class.extend({
		init: function(options) {
			var controllers = options.controllers,
				route = options.route,
				defaultModule = options.defaultModule;

			return {}
		}
	})

})